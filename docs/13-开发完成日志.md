# 开发完成日志

本文档只记录已经落地并验证过的内容；计划中的项目不记为完成。

## 2026-08-28

### 工程与本地开发基线

- 建立 iOS/iPadOS 16+ 的 XcodeGen 工程骨架、AppShell、基础导航和 Codemagic 构建配置。
- 修复单元测试宿主名称与产品名称不一致的问题，Codemagic 构建已通过。
- 在 Windows 开发机的 `yilai` 目录安装 Swift 6.3.3、本地 MSVC Build Tools 和 Windows SDK。
- 验证本地 Swift 样例可编译运行，并成功编译 `InventoryLedger.swift`。

### 豆仓领域核心

- 完成 SKU、库存流水、预留、采购在途、余额计算和溢出保护的领域模型。
- 完成入库、出库、盘点增减、冲销方向以及幂等键去重规则。
- 完成库存余额、可用量、在途量和预计可用量的单元测试覆盖。

### 豆仓本地闭环（第一版）

- 新增 Core Data SQLite 存储，库存流水以不可变记录保存，并在启动时重建余额。
- 新增豆仓总览：现存、可用、低库存数、色号卡片和最近流水。
- 新增色号详情页，展示现存、预留、可用、在途和该 SKU 的流水时间线。
- 新增入库、出库表单，提交前展示现存/可用预览；手工出库超过可用量会阻止写入。
- 新增应用层单元测试，覆盖写入后的汇总、超过可用量出库、重复幂等键三种场景。

### 本次验证

- Windows 静态验证脚本通过，确认 iOS 16/iPhone/iPad 工程约束未被破坏。
- 使用本地 Swift 6.3.3 工具链编译 `InventoryLedger.swift` 成功。
- Codemagic 已成功完成主应用的构建与单元测试；后续仅在一个完整大项完成后再触发云端构建。

### 独立豆仓（iOS 与飞牛 OS）

- 建立 `BeanWarehouseiOS/`：iOS/iPadOS 16+ 的独立 XcodeGen 工程，复用已验证的库存领域与 Core Data 仓储，并使用 `BeanWarehouse.sqlite` 与主应用数据隔离。
- 为独立 IPA 增加 Codemagic 手动工作流：`bean-warehouse-ios-validation` 用于集中验证，`bean-warehouse-ios-ipa-adhoc` 用于导出 Ad Hoc IPA。
- 建立 `BeanWarehouseNAS/`：本地 Node + SQLite 服务、中文响应式 Web 界面、Dockerfile 和 Compose 配置；支持入库、出库、低库存阈值、库存汇总与最近流水。
- 建立 `BeanWarehouse/contracts/inventory-ledger-v1.md`，明确 SKU、流水、幂等键、库存扣减与飞牛 OS HTTP 接口，防止 iOS/NAS 两端库存规则分叉。

### 独立豆仓验证

- NAS 服务的两项 Node 集成测试通过：入库/出库/幂等重试后的余额正确；超过可用库存的手工出库返回冲突。
- 独立 iOS 入口、库存模块和持久化模块通过本地 Swift 语法校验；主工程 Windows 静态验证再次通过。
- 当前 Windows 开发机未安装 Docker，因此 Compose 镜像构建与飞牛 OS 实机部署保留到首个完整豆仓大项结束后统一验证。
